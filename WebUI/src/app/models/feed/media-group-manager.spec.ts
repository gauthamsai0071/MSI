import { MediaGroupManager } from './media-group-manager';

describe('MediaGroupManager', () => {
  it('should create an instance', () => {
    expect(new MediaGroupManager()).toBeTruthy();
  });
});
